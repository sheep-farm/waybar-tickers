// Adicionar "custom/tickers" em modules-right (ou onde preferir)
// e copiar o bloco abaixo para o config.jsonc

"custom/tickers": {
    "exec": "~/.config/waybar/scripts/tickers.sh",
    "return-type": "json",
    "interval": 3
}
